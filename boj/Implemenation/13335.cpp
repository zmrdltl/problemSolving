#include <bits/stdc++.h>
using namespace std;
int n, w, l;
vector <int> truckWeight;
int main(){
  cin >> n >> w >> l;
  truckWeight.resize(n);
  for(int i = 0; i < n; i++) cin >> truckWeight[i];
  
}