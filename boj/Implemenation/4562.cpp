#include <bits/stdc++.h>
using namespace std;
int a, b, t;
int main(){
    cin >> t;
    while(t--){
        cin >> a >> b;
        if(a >= b) cout << "MMM BRAINS\n";
        else cout << "NO BRAINS\n";
    }
}